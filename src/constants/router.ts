interface IPath { root: string, contents: string, contentDetail: string, bookmarks: string }

export const paths: IPath = {
    root: '/',
    contents: '/contents',
    contentDetail: '/contents/detail',
    bookmarks: '/bookmarks',
}